

from random import random
import random
from traceback import print_tb
import numpy as np

"""# w = random between -100 and 100
W = np.zeros((2,5))

Y = np.zeros((2,5))

for i in range(Y.shape[0]):
    for j in range(Y.shape[1]):
        Y[i,j] = random.uniform(-1,1)

print(Y)
print(Y[0,:])
print(Y[1,:])
print(Y[:,1])
"""
"""NV = 2
N = 5


X = np.zeros((NV,N))

print(X.shape)"""

df = np.zeros((2,5))

"""
# add random values to df
for i in range(df.shape[0]):
    for j in range(df.shape[1]):
        df[i,j] = random.uniform(-1,1)

print(df)

#X[:, I] = X[:, ChosenMutation]*aux

df[0,0] = df[0,0]*2
print(df)
"""

for i in range(2,2):
    print(i)